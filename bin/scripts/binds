# input configuration goes here
bind w +forward
bind s +backward
bind a +left
bind d +right
bind r respawn
bind space jump
bind l noclip
bind n nazi
bind ; 'save_pos cam_pos'
bind p 'load_pos cam_pos'
bind q 'cam.rot z -5'
bind e 'cam.rot z +5'
bind f2 screenshot
bind esc quit
bind k +test